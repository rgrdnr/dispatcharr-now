services:
  dispatcharr-now:
    build: .
    container_name: dispatcharr-now
    restart: unless-stopped
    ports:
      - "8790:8790"
    env_file:
      - .env
    volumes:
      - dispatcharr-now-data:/app/server/data

volumes:
  dispatcharr-now-data:
